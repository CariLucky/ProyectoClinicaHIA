[mysqld]
server-id=2
log_bin=mysql-bin
relay_log_recovery=ON
binlog_format=row
expire_logs_days=7
max_binlog_size=100M

# Configuración de GTID (¡Esto es lo que faltaba o no se cargó!)
gtid_mode = ON
enforce_gtid_consistency = ON

default_authentication_plugin = mysql_native_password